📘 Инструкция по запуску и использованию проекта qa_auth

1. Что это за проект

qa_auth — это backend-сервер на FastAPI, который реализует:
 • регистрацию пользователя
 • аутентификацию по контрольному вопросу и ответу
 • хранение данных в базе (SQLite + SQLAlchemy)
 • хеширование ответов (bcrypt)

Сервер запускается локально и предоставляет HTTP API.

⸻

2. Требования

Перед началом убедись, что установлено:
 • Windows
 • Python 3.10+
 • Доступ к командной строке (cmd или PowerShell)

Проверка Python:

python --version


⸻

3. Создание структуры проекта

3.1 Создай папку проекта

mkdir qa_auth
cd qa_auth

3.2 Структура проекта должна быть такой

qa_auth/
│
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── database.py
│   ├── models.py
│   ├── hashing.py
│
├── venv/
│
├── requirements.txt
└── README.md


⸻

4. Создание виртуального окружения (venv)

В папке qa_auth выполни:

python -m venv venv

Активация venv

venv\Scripts\activate

Если всё ок — в начале строки появится:

(venv)


⸻

5. Установка зависимостей

5.1 Создай файл requirements.txt

fastapi
uvicorn
sqlalchemy
bcrypt
pydantic

5.2 Установи зависимости

pip install -r requirements.txt


⸻

6. Файлы проекта (кратко)

database.py
 • создаёт подключение к SQLite
 • управляет сессиями БД

models.py
 • описывает таблицы:
 • User
 • UserQuestion

hashing.py
 • хеширование вопрос + ответ
 • проверка ответа при логине

main.py
 • основной файл сервера
 • все эндпоинты (/register, /login/*)

⸻

7. Запуск сервера

Из корня проекта (qa_auth):

python -m uvicorn app.main:app --reload

Если всё успешно, ты увидишь:

Uvicorn running on http://127.0.0.1:8000


⸻

8. Интерфейс сервера (Swagger)

Открой в браузере:

http://127.0.0.1:8000/docs

Это интерактивный интерфейс, где можно:
 • отправлять запросы
 • смотреть ответы
 • тестировать сервер без Postman

⸻

9. Как пользоваться сервером

9.1 Регистрация пользователя

POST /register

Пример тела запроса (JSON):

{
  "username": "ivan",
  "question": "Имя моего первого питомца?",
  "answer": "Барсик"
}

Ожидаемый ответ:

{
  "статус": "зарегистрирован"
}


⸻

9.2 Запрос контрольных вопросов

POST /login/request

{
  "username": "ivan"
}

Ответ:

{
  "questions": [
    {
      "id": 1,
      "question": "Имя моего первого питомца?"
    }
  ]
}

📌 id вопроса понадобится на следующем шаге.

⸻

9.3 Проверка ответа (аутентификация)

POST /login/verify

{
  "username": "ivan",
  "question_id": 1,
  "answer": "Барсик"
}

Успешный ответ:

{
  "статус": "успешно аутентифицировано"
}

Ошибки:
 • неверный ответ → 401
 • неверный вопрос → 400
 • пользователь не найден → 404

⸻

10. Частые проблемы и решения

❌ uvicorn не является командой

✔ Используй:

python -m uvicorn app.main:app --reload


⸻

❌ null в ответе

✔ Значит в эндпоинте нет return
✔ Проверяй отступы и финальный return

⸻

❌ password cannot be longer than 72 bytes

✔ Ответ или вопрос слишком длинный
✔ Нужно обрезать строку до 72 байт перед bcrypt

⸻

11. Как остановить сервер

В терминале:

CTRL + C


⸻

12. Что можно улучшить дальше
 • JWT токены
 • ограничение попыток входа
 • несколько контрольных вопросов
 • логирование
 • PostgreSQL вместо SQLite
 • frontend